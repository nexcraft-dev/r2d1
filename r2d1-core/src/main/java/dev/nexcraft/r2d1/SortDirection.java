package dev.nexcraft.r2d1;

/** Direction used when sorting query results by one indexed field. */
public enum SortDirection {
  /** Sorts values from lowest to highest. */
  ASC,

  /** Sorts values from highest to lowest. */
  DESC
}
